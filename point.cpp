#include "point.h"

/*template <typename T>
void point <T> ::move (T delta_X, T delta_Y) {
    x += delta_X;
    y += delta_Y;
}*/

void point::move (int delta_X, int delta_Y) {
    x += delta_X;
    y += delta_Y;
}